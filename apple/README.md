# Apple application

Native SwiftUI/Xcode shell for the Ancient North Arabian/NLP toolkit. Unicode/script registries and deterministic classification logic remain portable; heavyweight Python/ML processing stays behind a service/native boundary rather than being silently bundled into an IPA.

Targets: iOS/iPadOS and macOS. IPA export requires macOS/Xcode.
