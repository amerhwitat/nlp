# Assembly layer

Architecture-specific assembly is reserved for verified, non-security-sensitive primitives such as optimized memory operations, hashing adapters and ABI glue.

No assembly routine in FlashTool bypasses Android bootloader, secure-boot, FRP or authentication controls.
